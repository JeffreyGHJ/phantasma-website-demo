import styled from 'styled-components';

const AssetCardGroup = styled.section`
	display: flex;
	flex-direction: row;
	justify-content: flex-start;
	gap: 2rem;
    fontSize:16px;
	flex-wrap: wrap;
`;

export default AssetCardGroup;
