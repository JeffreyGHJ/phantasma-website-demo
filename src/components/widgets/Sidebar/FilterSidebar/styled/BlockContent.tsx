import styled from 'styled-components';

const BlockContent = styled.div`
	padding: 1rem 1rem;
	border-bottom: 1px solid var(--border-color-divider);

	.widget.AttributeAccordionDetails {
		max-height: 300px;
	}
`;

export default BlockContent;
