import styled from 'styled-components';

const SizeSlider = styled.div`
	min-width: 200px;
	& > div {
		margin-bottom: 0px;
	}

	.slider-label {
		font-size: 1.5rem;
	}
`;

export default SizeSlider;
